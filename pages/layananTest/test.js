const search = document.getElementById('search');
search.addEventListener('click', (e) => {
    e.preventDefault();
    alert('Fitur ini belum tersedia')
})

const submitTest = document.getElementById('submit__test');
submitTest.addEventListener('click', (e) => {
    e.preventDefault();
    alert('Fitur ini belum tersedia')
});